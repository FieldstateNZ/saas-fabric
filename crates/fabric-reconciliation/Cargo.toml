[package]
name = "fabric-reconciliation"
description = "Converges a client's desired identity state onto an identity provider. Owns comparison and convergence; owns no provider protocol."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
fabric-client-model.workspace = true
fabric-core.workspace = true

async-trait.workspace = true
serde.workspace = true
thiserror.workspace = true
tracing.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }

[lints]
workspace = true
